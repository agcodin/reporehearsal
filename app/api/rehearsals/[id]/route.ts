import { NextResponse } from "next/server";import{getRehearsal}from"../../../../src/rehearsals/session-service";import{sessionRequest,rehearsalFailure}from"../_shared";
export async function GET(request:Request,{params}:{params:Promise<{id:string}>}){try{const{id}=await params;const{user,token}=await sessionRequest(request);return NextResponse.json({session:await getRehearsal(id,user,token)})}catch(error){return rehearsalFailure(error)}}
