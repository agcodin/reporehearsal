import Link from "next/link";
import { Suspense } from "react";
import { requireChatGPTUser } from "../chatgpt-auth";
import { getAccountDashboard } from "../../src/accounts/account-service";

export const dynamic = "force-dynamic";

function metric(value: number | null, suffix = "") { return value === null ? "—" : `${value}${suffix}`; }

async function DashboardContent() {
  const user = await requireChatGPTUser("/dashboard");
  const data = await getAccountDashboard(user.email, user.displayName);
  const firstName = user.fullName?.split(" ")[0] ?? user.email.split("@")[0];
  const recent = data.rehearsals.slice(0, 6);
  return <main className="app-page"><div className="page-title-row"><div><p className="eyebrow">WELCOME BACK, {firstName.toUpperCase()}</p><h1>Incident readiness</h1><p>{data.metrics.completed ? `${data.metrics.completed} completed rehearsal${data.metrics.completed === 1 ? "" : "s"}. Your history is saved to this account.` : "Your readiness profile begins with your first completed rehearsal."}</p></div><Link className="button button-dark" href="/rehearsals/new">Start new rehearsal →</Link></div><section className="grid-4"><div className="stat-card"><small>AVERAGE SCORE</small><strong>{metric(data.metrics.averageScore)}</strong><span className="delta">Completed rehearsals</span></div><div className="stat-card"><small>AVG. DIAGNOSIS</small><strong>{metric(data.metrics.averageDuration, "m")}</strong><span className="delta">Measured after submission</span></div><div className="stat-card"><small>COMPLETION RATE</small><strong>{metric(data.metrics.completionRate, "%")}</strong><span className="delta">{data.rehearsals.length} started</span></div><div className="stat-card"><small>TOP SKILL GAP</small><strong style={{ fontSize: 20 }}>{data.metrics.topSkillGap}</strong><span className="delta">Based on saved results</span></div></section><div className="dashboard-grid"><section className="panel"><span className="panel-label">REHEARSAL HISTORY</span><h2>Your investigations</h2>{recent.length ? recent.map(session => <div className="session-row" key={session.id}><div><b>{session.incidentName}</b><p>{session.repositoryName} · {new Date(session.completedAt).toLocaleDateString()}</p></div><span>{session.durationMinutes}m</span><strong className="score-ring">{session.score}</strong><span>{session.status === "COMPLETED" ? "✓" : "×"}</span></div>) : <div className="empty-state"><span className="step-icon">01</span><h3>No rehearsals yet</h3><p>Start with the billing migration incident. Your score, duration, hints, and completion status will appear here after submission.</p><Link className="button button-blue" href="/rehearsals/new">Choose your first incident →</Link></div>}</section><aside><section className="panel recommend"><span className="panel-label">RECOMMENDED NEXT</span><h2>{data.metrics.completed ? "Container host mismatch" : "Required field migration"}</h2><span className="badge badge-red">{data.metrics.completed ? "BEGINNER" : "INTERMEDIATE"}</span><p>{data.metrics.completed ? "Practice reasoning across service network boundaries without relying on application code changes." : "Build a complete evidence trail through logs, data comparison, migration history, and regression validation."}</p><Link className="button button-accent" href="/rehearsals/new">Start recommended exercise →</Link></section><section className="panel" style={{ marginTop: 18 }}><span className="panel-label">ACCOUNT DEFAULTS</span><h2>How you rehearse</h2><p><b>Mode</b><br />{data.profile.defaultMode.toLowerCase().replace(/^./, value => value.toUpperCase())}</p><p><b>Time limit</b><br />{data.profile.defaultTimeLimit} minutes</p><Link className="text-link" href="/account">Update account settings →</Link></section></aside></div></main>;
}

export default function Dashboard() { return <Suspense fallback={<main className="app-page"><div className="account-loading"><span className="pulse" /> Loading your readiness profile…</div></main>}><DashboardContent /></Suspense>; }
