export default function Privacy(){return <main className="prose-page"><p className="eyebrow">SECURITY & PRIVACY</p><h1>Your repository stays yours.</h1><p>The original repository is never modified. RepoRehearsal creates a unique temporary copy for each session and deletes it after expiry.</p><h2>Accounts</h2><ul><li>Sign-in is optional. ChatGPT provides the current identity; RepoRehearsal never receives or stores your password.</li><li>Your account stores email, display name, rehearsal preferences, result summaries, and metadata for repositories you analyze while signed in.</li><li>Editor drafts, terminal output, repository contents, secrets, provider tokens, and raw investigation notes are not stored in your account profile.</li><li>The public demo, pasted public GitHub links, and local uploads work without an account.</li></ul><h2>Repository handling</h2><ul><li>Public GitHub analysis reads only limited repository metadata and never writes to the source.</li><li>Folder and ZIP uploads are limited to 20 MB and 3,000 files. Generated folders, binaries, oversized files, environment files, private keys, and credential files are excluded.</li><li>Uploaded code is analyzed for the current request; only non-content metadata is added to a signed-in account.</li><li>Only limited, relevant excerpts are sent to an AI model when enhancement is enabled.</li></ul><h2>Sandbox boundaries</h2><ul><li>Commands are selected by approved IDs; arbitrary shell strings are rejected.</li><li>File paths are normalized and contained inside the session workspace.</li><li>Outbound networking, privileged containers, Docker socket mounts, and host paths are blocked.</li><li>Hidden tests remain outside the exercise workspace.</li></ul><h2>Retention</h2><p>Temporary workspaces have a maximum lifetime of 45 minutes. Durable account history contains identity, preferences, repository metadata, and result summaries. Events store action categories and safe summaries—not tokens, environment values, passwords, repository contents, or unredacted terminal history.</p></main>}
