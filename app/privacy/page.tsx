export default function Privacy() {
  return <main className="prose-page"><p className="eyebrow">SECURITY & PRIVACY</p><h1>Your repository stays yours.</h1><p>The original repository is never modified. RepoRehearsal stores a filtered source snapshot, then creates a separate working copy for each rehearsal.</p>
    <h2>Accounts</h2><ul><li>Sign-in is optional. ChatGPT provides the current identity; RepoRehearsal never receives or stores your password.</li><li>Account data includes email, display name, preferences, result summaries, repository metadata, and reusable safe source snapshots.</li><li>External provider passwords and tokens are never stored.</li><li>The public demo, pasted public GitHub links, and local uploads work without an account.</li></ul>
    <h2>Repository handling</h2><ul><li>Public GitHub import is read-only and never writes to the source repository.</li><li>Folder and ZIP uploads are limited to 20 MB and 3,000 files. Generated folders, binaries, oversized files, environment files, private keys, and credential files are excluded.</li><li>Anonymous source snapshots expire after 24 hours. Signed-in snapshots remain reusable until removed.</li><li>Each rehearsal receives its own R2-backed working copy. Completed and expired working copies are deleted.</li><li>Only deterministic results and safe investigation summaries—not repository text—are sent to OpenAI when report enhancement is configured.</li></ul>
    <h2>Execution boundaries</h2><ul><li>The hosted worker accepts approved command IDs and returns scenario-specific deterministic checks; it does not execute arbitrary uploaded code.</li><li>File paths are normalized and contained inside the session workspace.</li><li>No repository-controlled network requests, package installs, shell strings, or privileged containers are exposed.</li><li>Hidden validation rules remain outside the exercise workspace.</li></ul>
    <h2>Retention</h2><p>Active workspaces expire at the configured time limit. Anonymous repository snapshots expire after 24 hours; an authenticated cleanup endpoint supports scheduled deletion. Account-owned source snapshots and completed reports are durable until removed under the account. Timeline events and hypotheses are stored with the rehearsal to produce the report.</p>
  </main>;
}
