const guarantees=[["Temporary copies","Workspaces expire automatically after the rehearsal or configured retention window."],["Redacted secrets","Environment values, keys, credentials, and private material are removed before injection."],["Allowlisted commands","Only supported diagnostic and verification operations can run inside the workspace."],["Read-only source","The original repository is never modified by a rehearsal."]];
const details=[["How workspaces are created and destroyed","A filtered source snapshot is copied into an isolated workspace. Completed workspaces are deleted; unfinished work expires automatically."],["What we redact","Environment files, private keys, credentials, binary assets, generated dependencies, and oversized files are excluded from analysis."],["Command allowlisting","The workspace exposes approved command IDs rather than a general-purpose shell. Package installs, outbound repository-controlled networking, and privileged execution stay blocked."],["Data retention","Anonymous snapshots expire after 24 hours. Signed-in repository snapshots and reports remain reusable until they are removed from the account."],["What we never do","We never write to the original repository, store provider passwords or OAuth access tokens, or use uploaded source to train a model."]];
export default function Privacy(){return <main className="privacy-spec rr-narrow"><header><p>TRUST</p><h1>Your repository stays yours.</h1><span>Every exercise runs against a temporary copy. Here is exactly what that means.</span></header><section className="guarantee-grid">{guarantees.map(([title,copy])=><article key={title}><h4>{title}</h4><p>{copy}</p></article>)}</section><section className="privacy-details">{details.map(([title,copy])=><article key={title}><h3>{title}</h3><p>{copy}</p></article>)}</section><aside>Original repositories are never modified.</aside><p className="security-contact">Questions? <a href="https://github.com/agcodin/reporehearsal/issues" target="_blank" rel="noreferrer">Open a security or privacy inquiry ↗</a></p></main>}
