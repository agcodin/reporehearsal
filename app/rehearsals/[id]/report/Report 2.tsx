"use client";
import Link from "next/link"; import { useSearchParams } from "next/navigation"; import { evaluateRepair } from "../../../../src/evaluation/scoring";
export default function Report(){const params=useSearchParams(); const browser=typeof window!=="undefined"; const source=browser?sessionStorage.getItem("rr-source")??"":""; const hints=browser?Number(sessionStorage.getItem("rr-hints")??0):0; const result=evaluateRepair(source,hints); const passed=params.get("passed")!=="false"&&result.passed; const score=passed?result.score:Math.min(result.score,54); const markdown=`# RepoRehearsal after-action report\n\n**Score:** ${score}/100\n\n## Root cause\nThe partner-import path omitted billingRegion after the schema transition, and serialization assumed the field was always present.\n\n## Repair\n${passed?"Passed public and hidden validation.":"Did not pass hidden validation."}\n`; function download(){const a=document.createElement("a");a.href=URL.createObjectURL(new Blob([markdown],{type:"text/markdown"}));a.download="reporehearsal-after-action.md";a.click();URL.revokeObjectURL(a.href)}return <main><section className="report-hero"><div className="report-inner"><span className="report-kicker">REHEARSAL COMPLETE · {passed?"INCIDENT RESOLVED":"REPAIR INCOMPLETE"}</span><div className="score-layout"><div><div className="big-score">{score}<span>/100</span></div><span className={`badge ${passed?"badge-green":"badge-red"}`}>{passed?"STRONG RESPONSE":"NEEDS REVISION"}</span></div><div><h1>{passed?"You repaired the cause—not just the symptom.":"The visible symptom may remain."}</h1><p>{passed?"The change protects legacy data, fixes the secondary creation path, and preserves the billing-region constraint.":"Review the failed validation and return to the workspace with a root-cause repair."}</p><div className="breakdown">{result.breakdown.map(b=><div key={b.label}><small>{b.label.toUpperCase()}</small><b>{passed?b.earned:Math.min(b.earned,Math.floor(b.possible*.55))}/{b.possible}</b></div>)}</div></div></div></div></section><div className="report-body"><div><section className="report-section"><h2>Root cause</h2><p>The migration introduced <code>billing_region</code> as a required field, but the partner-import account path still created billing profiles without it. The billing serializer called <code>toUpperCase()</code> on the missing value, causing account-specific HTTP 500 responses.</p><div className="callout"><b>Root cause ≠ symptom</b><p>The null serialization exception was the visible mechanism. The incomplete schema transition across creation paths was the underlying cause.</p></div></section><section className="report-section"><h2>Deterministic validation</h2>{result.checks.map(c=><div className="check-row" key={c.name}><span>{c.status==="passed"?"✓":"×"}</span><div><b>{c.name} {c.hidden&&<small>· HIDDEN</small>}</b><p>{c.detail}</p></div></div>)}</section><section className="report-section"><h2>Prevention measures</h2><ol><li>Add a migration contract test against both clean and legacy snapshots.</li><li>Use expand/backfill/contract sequencing for newly required columns.</li><li>Monitor the null rate and account-creation source during rollout.</li><li>Require fixtures for every account-creation path in billing regression tests.</li></ol></section></div><aside><section className="report-section"><h2>Investigation review</h2><p><b>Evidence used</b><br/>Correlated request log, failing HTTP response, working/failing database comparison, public tests.</p><p><b>Missed evidence</b><br/>Migration history could have been inspected earlier.</p><p><b>Hints</b><br/>{hints} requested · {hints===0?"full independence":"independence deduction applied"}</p></section><section className="report-section"><h2>Recommended next</h2><span className="badge badge-blue">CONFIGURATION</span><h3>Container host mismatch</h3><p>Build confidence reasoning about service network boundaries.</p><Link className="button button-dark" href="/rehearsals/new">Choose next exercise →</Link></section><button className="button button-ghost" style={{width:"100%"}} onClick={download}>Export Markdown ↓</button></aside></div></main>}
