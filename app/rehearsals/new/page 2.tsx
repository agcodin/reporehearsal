import Link from "next/link"; import { incidents } from "../../../src/data";
export default function NewRehearsal(){return <main className="app-page"><div className="page-title-row"><div><p className="eyebrow">NEW REHEARSAL · STEP 2 OF 2</p><h1>Select an incident</h1><p>Compatible with RepoRehearsal Billing Demo.</p></div></div><div className="scenario-grid">{incidents.map((i,index)=><article className="scenario-card" key={i.id}><span className={`badge ${index===0?"badge-red":"badge-blue"}`}>{i.difficulty.toUpperCase()}</span><h2>{i.name}</h2><p>{i.summary}</p><div className="tag-row"><span className="tag">{i.category.replace("_"," ")}</span><span className="tag">{index===1?"10–15 min":"15–25 min"}</span><span className="tag">Guided</span></div><Link className={`button ${index===0?"button-dark":"button-ghost"}`} href={`/rehearsals/demo/preparing?incident=${i.id}`}>{index===0?"Start polished demo":"Start scenario"} →</Link></article>)}</div><section className="panel" style={{marginTop:18}}><span className="panel-label">EXERCISE SETTINGS</span><div className="grid-4" style={{marginTop:16}}><label>Difficulty<select defaultValue="INTERMEDIATE"><option>BEGINNER</option><option>INTERMEDIATE</option><option disabled>ADVANCED · PREVIEW</option></select></label><label>Mode<select defaultValue="GUIDED"><option>GUIDED</option><option>INDEPENDENT</option></select></label><label>Timer<select defaultValue="UNTIMED"><option>UNTIMED</option><option>25 MINUTES</option></select></label><label>Repository<select><option>Billing Demo</option></select></label></div></section></main>}
