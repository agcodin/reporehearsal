import express from "express";
export type BillingProfile={organizationId:string;displayName:string;billingRegion:string};
export const profiles:BillingProfile[]=[{organizationId:"org_legacy_01",displayName:"Northstar Labs",billingRegion:"US"},{organizationId:"org_legacy_02",displayName:"Aster Systems",billingRegion:"EU"},{organizationId:"org_new_01",displayName:"Juniper Works",billingRegion:"US"},{organizationId:"org_new_02",displayName:"Morrow & Co",billingRegion:"APAC"}];
export function createApp(){const app=express();app.use(express.json());app.get("/health",(_,res)=>res.json({status:"healthy",service:"billing-api"}));app.get("/ready",(_,res)=>res.json({status:"ready",database:"connected"}));app.get("/api/organizations/:id/billing",(req,res)=>{const requestId=`req_${Date.now().toString(36)}`;const profile=profiles.find(p=>p.organizationId===req.params.id);if(!profile)return res.status(404).json({code:"NOT_FOUND",requestId});console.log(JSON.stringify({timestamp:new Date().toISOString(),level:"info",service:"billing-api",requestId,route:req.path,organizationId:req.params.id,durationMs:4}));return res.json({displayName:profile.displayName,billingRegion:profile.billingRegion.toUpperCase()})});app.post("/api/organizations",(req,res)=>{const created={organizationId:`org_${profiles.length+1}`,displayName:req.body.name,billingRegion:req.body.billingRegion??"US"};profiles.push(created);res.status(201).json(created)});return app;}
