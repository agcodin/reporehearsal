import { describe,expect,it } from "vitest"; import { analyzeRepository } from "../../src/repositories/analyzer"; import { incidents,repositoryMap,safeBillingSource } from "../../src/data"; import { isCompatible } from "../../src/incidents/compatibility"; import { evaluateRepair } from "../../src/evaluation/scoring";
describe("complete demo incident",()=>{it("analyzes, selects, and validates the supported repository",()=>{const analysis=analyzeRepository("billing-demo","Billing Demo",[{path:"package.json",content:'{"dependencies":{"express":"5"},"devDependencies":{"vitest":"3"}}'},{path:"package-lock.json",content:""},{path:"src/server.ts",content:""},{path:"prisma/schema.prisma",content:'datasource db { provider = "postgresql" }'},{path:"prisma/migrations/001/migration.sql",content:""},{path:"tests/billing.test.ts",content:""},{path:"Dockerfile",content:""}]);expect(analysis).toMatchObject({language:"TypeScript",framework:"Express",database:"PostgreSQL",orm:"Prisma",testFramework:"Vitest",containerized:true});expect(isCompatible(incidents[0],repositoryMap)).toBe(true);const validation=evaluateRepair(safeBillingSource);expect(validation.passed).toBe(true);expect(validation.checks.filter(c=>c.hidden).every(c=>c.status==="passed")).toBe(true)})});
