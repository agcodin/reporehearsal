import { describe,expect,it } from "vitest"; import { isSafeArchiveEntry,resolveWorkspacePath } from "../../src/security/path-validation"; import { commandFor } from "../../src/sandbox/command-policy"; import { redactSecrets } from "../../src/security/secret-redaction";
describe("sandbox security",()=>{it("contains workspace paths",()=>{expect(resolveWorkspacePath("/tmp/session","src/app.ts")).toBe("/tmp/session/src/app.ts");expect(()=>resolveWorkspacePath("/tmp/session","../secret")).toThrow(/escapes/);expect(()=>resolveWorkspacePath("/tmp/session","/etc/passwd")).toThrow(/Invalid/)});it("rejects unsafe archive entries",()=>{expect(isSafeArchiveEntry("src/app.ts")).toBe(true);expect(isSafeArchiveEntry("../escape")).toBe(false);expect(isSafeArchiveEntry("/absolute")).toBe(false);expect(isSafeArchiveEntry("src\\..\\escape")).toBe(false)});it("uses approved command IDs",()=>{expect(commandFor("run-tests")).toEqual(["npm","test"]);expect(()=>commandFor("rm-all")).toThrow(/not approved/)});it("redacts common secrets",()=>expect(redactSecrets("OPENAI_API_KEY=sk-live Bearer abc.def")).toBe("OPENAI_API_KEY=[REDACTED] [REDACTED]"))});
